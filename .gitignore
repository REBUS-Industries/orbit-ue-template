# Unreal Engine generated / per-machine artefacts
Binaries/
DerivedDataCache/
Intermediate/
Saved/

# Visual Studio / Rider / Xcode IDE files
*.VC.db
*.opensdf
*.sdf
*.sln
*.suo
*.xcodeproj
*.xcworkspace
.vs/
.idea/
.vscode/

# Build outputs
Build/
*.opendb

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Local CI / packaging
dist/
RELEASE_NOTES.md

# v1.0.130 -- per-release build logs (we redirect `Build.bat` output to
# `build-<version>.log` for diagnostics; ephemeral, never useful in
# git history -- the in-tree README + commit message already document
# the build outcome).
*.log
